.fixed.bg-blue-400.w-full.h-16.inline-flex.items-center.px-4.shadow.top-0
  .text-white.text-5xl.font-medium.italic.flex-grow
    = link_to "Fåcebook", root_path
  - if user_signed_in?
    #bell
      = render "users/buttons/notifications", enable: current_user.notifications.any?
    #notifications.hidden
      = render "users/notifications", notifications: current_user.notifications
    .text-white.text-lg.mr-5
      = link_to "Sign out", destroy_user_session_path, method: :delete
    = link_to user_path(current_user) do
      .flex.gap-2
        = image_tag(url_for(current_user.avatar_thumbnail))
        .text-white.text-2xl.font-medium.self-end
          = current_user.first_name
